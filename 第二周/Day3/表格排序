1. 获取数据（ajax）
2. 绑定数据（字符串拼接）
3. 循环绑定事件（th）
4. 排序
（1）将要排序的tr 转化为数组 使用 数组的sort方法进行排序
（2）数字排序(根据每列内容) return a - b * that.flag
（3）汉字排序(tr 上保存的拼音) return pin1.localeCompare(pin2) * that.flag
（4）升降序切换 （给每一个th保存 flag=-1）
5.隔行变色（注意每次排序后 需要重新计算变色）